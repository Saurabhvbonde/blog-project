spring.application.name=backend

# ---- Server ----
server.port=8081

# ---- MySQL datasource ----
spring.datasource.url=jdbc:mysql://localhost:3306/blognewdb?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=7709798256
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ---- JPA / Hibernate ----
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ---- JWT ----
app.jwt.secret=change-this-super-secret-key-to-a-long-random-value-before-production-use
app.jwt.expiration-ms=86400000

# ---- Seed admin account (created on startup if it doesn't exist) ----
app.seed.admin.username=admin
app.seed.admin.password=Admin@123
app.seed.admin.email=admin@blog.com
